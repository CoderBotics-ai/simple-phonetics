<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>SearchPhonetics</groupId>
  <artifactId>SearchPhonetics</artifactId>
  <version>0.0.1-SNAPSHOT</version>

  <properties>
    <!-- Define Java version property for consistency - Upgraded to Java 21 -->
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Define JUnit Jupiter version property - Already at 5.9.2 as requested -->
    <junit.jupiter.version>5.9.2</junit.jupiter.version>
  </properties>

  <dependencies>
        <!-- Upgraded JUnit from version 4 to 5 (Jupiter) -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.jupiter.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.platform</groupId>
            <artifactId>junit-platform-commons</artifactId>
            <version>1.9.2</version>
        </dependency>
    </dependencies>

  <build>
    <resources>
      <resource>
        <directory>src/main/resource</directory>
        <excludes>
          <exclude>**/*.java</exclude>
        </excludes>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <!-- Updated Maven Compiler Plugin configuration for Java 21 -->
        <artifactId>maven-compiler-plugin</artifactId>
        <!-- Using version 3.11.0 (meets requirement of 3.10.1+) -->
        <version>3.11.0</version>
        <configuration>
          <!-- Set target release compatibility to Java 21 using property -->
          <!-- Using <release> is preferred over <source>/<target> for modern Java versions -->
          <release>${java.version}</release>
        </configuration>
      </plugin>
      <plugin>
        <!-- Updated Surefire plugin configuration for Java 21 test execution and JUnit 5 support -->
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <!-- Using version 3.1.2 (meets requirement of 3.0.0-M8+) -->
        <version>3.1.2</version>
      </plugin>
    </plugins>
  </build>
</project>